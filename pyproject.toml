[tool.ruff]
target-version = "py312"
line-length = 120
extend-exclude = [
  "apps/web/node_modules",
  "apps/web/dist",
  "services/api/.venv",
  ".pytest_cache",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = ["E501"]
